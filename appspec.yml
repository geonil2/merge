version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/merge-frontend

permissions:
  - object: /home/ubuntu/merge-frontend
    pattern: "**"
    owner: ubuntu
    group: ubuntu
    mod: 755

hooks:
  ApplicationStart:
    - location: scripts/application_start.sh
      timeout: 300
      runas: ubuntu
  ApplicationStop:
    - location: scripts/application_stop.sh
      timeout: 300
      runas: ubuntu
