import React from 'react';
import styled from "@emotion/styled";
import Image from "next/image";

const BigImageTableList = () => {
  return (
    <List>
      {/*<Image src={} />*/}
      <p></p>
      <div></div>
    </List>
  );
};

const List = styled.div`

`

export default BigImageTableList;
